<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ADMIN PANEL</title>
    <style>
        table, th, td {
            margin-left: 100px;
            margin-top: 50px;
          border:1px solid red;
        }
        </style>
</head>
<body>
    <h1>Registered Users</h1>
    <table style="width: 80%;">
        <tr>
            <th>First name</th> 
            <th>Last Name</th>
            <th>Email Id</th>
            <th>Password</th>
        </tr>   
        <% for (let user of users) {%>
            <tr>
                <td style="text-align: center;"><%= user.firstname %> </td>
                <td style="text-align: center;"><%= user.lastname %> </td>
                <td style="text-align: center;"><%= user.email %> </td>
                <td style="text-align: center;"><%= user.password %> </td>
            </tr>
            <% } %>  
        </table>
        <form action="/makecsv" method="get"><button type="submit">Create CSV of Users</button></form>
        <br>
        <h1>Users Messages</h1>
    <table style="width: 80%;">
        <tr>
            <th>User Name</th> 
            <th>Email</th>
            <th>Company</th>
            <th>Phone</th>
            <th>Address</th>
        </tr>   
        <% for (let u of mess) {%>
            <tr>
                <td style="text-align: center;"><%= u.name %> </td>
                <td style="text-align: center;"><%= u.email %> </td>
                <td style="text-align: center;"><%= u.company%> </td>
                <td style="text-align: center;"><%= u.phone %> </td>
                <td style="text-align: center;"><%= u.address %> </td>
            </tr>
            <% } %>  
        </table>
        <form action="/makeusercsv" method="get"><button type="submit">Create CSV of User's Messages</button></form>
        <h1>Users Comments</h1>
        <table style="width: 80%;">
            <tr>
                <th>User Name</th> 
                <th>Email</th>
                <th>Comment</th>
            </tr>   
            <% for (let comment of comments) {%>
                <tr>
                    <td style="text-align: center;"><%= comment.username %> </td>
                    <td style="text-align: center;"><%= comment.email %> </td>
                    <td style="text-align: center;"><%= comment.comment%> </td>
                </tr>
                <% } %>  
            </table>
            <form action="/makecommentcsv" method="get"><button type="submit">Create CSV of User's Comment</button></form>
    </body>
</html>